<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapsql">
	<select id="searchplacelist" parameterType="String" resultType="vo">
		select * from placelist 
		<trim prefix="where">
			<if test="value!=null">
			address like '%'||#{value}||'%'
			</if>
		</trim>
	</select>
	  
   <insert id="wishadd" parameterType="vo">
   insert into favo values (1,#{info_id},#{member_id},sysdate)
   </insert>
   
   <select id="wishcheck" parameterType="vo" resultType="int">
   select count(*) from favo where info_id = #{info_id} and member_id=#{member_id}
   </select>
   
   <select id="wishrem" parameterType="vo" resultType="String">
   select distinct placename,info_id from favo,member2,placelist where member2.id = #{member_id} and favo.member_id = member2.id and placelist.tel = favo.info_id
   </select>
   
   <delete id="deletewish" parameterType="vo">
      delete from favo where info_id = #{info_id} and member_id=#{member_id}
   </delete>
   
	
</mapper>